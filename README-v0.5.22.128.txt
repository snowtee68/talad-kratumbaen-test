V0.5.22.128 – Notification Role Routing Fix

1) Rider push: ?rider_jobs=1 is now authoritative and is never rewritten to seller/auto.
2) Seller new-order push: opens seller order page directly and forces the Action / รอดำเนินการ tab in one tap.
3) No SQL change required for this release.
4) Deploy the full package, then fully close/reopen the PWA once so sw.js V0.5.22.128-r1 activates.
