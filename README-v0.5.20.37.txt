V0.5.20.37 – Seller Order Entry Fix
- เจ้าของร้านกดปุ่ม “ออเดอร์” -> เข้า Seller Inbox โดยตรง
- ถ้ามีร้านเดียว -> เปิดร้านนั้นและแท็บ “ต้องทำตอนนี้” ทันที
- In-app notification banner ใช้ flow เดียวกัน
- Deep link ที่มี order_id แม้ไม่มี order_tab=seller จะตรวจว่า user เป็นเจ้าของร้านหรือไม่ แล้วเปิดฝั่ง Seller ให้อัตโนมัติ
- Guest/ลูกค้าทั่วไปยังเปิด “ออเดอร์ที่ฉันสั่ง” ตามเดิม
- ใช้ RPC V0.5.20.35 เดิม ไม่มี SQL ใหม่
